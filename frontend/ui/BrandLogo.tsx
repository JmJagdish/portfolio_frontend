import { brandFont } from "@/app/fonts";

export default function BrandLogo() {
    return (
        <h1 className={`${brandFont.className} text-4xl text-primary`}>
            Jagdish
        </h1>
    );
}